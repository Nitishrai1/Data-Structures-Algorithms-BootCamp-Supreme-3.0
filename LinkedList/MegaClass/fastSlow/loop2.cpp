// wap toi return the head  of the loop in linked list
#include<bits/stdc++.h>
using namespace std;
//  ListNode *detectCycle(ListNode *head) {
//         ListNode* slow=head;
//         ListNode* fast=head;
//         if(head==NULL)return nullptr;
//         while(fast && fast->next){
//             slow=slow->next;
//             fast=fast->next->next;
//             if(fast==slow){
//                 break;
//             }
//         }
//         if(!fast || !fast->next){
//             return nullptr;
//         }
//         // ab slow or fast same pos pe point kar rahe hai

//         slow=head;
//         while(slow!=fast){
//             slow=slow->next;
//             fast=fast->next;
//         }
//         return slow;
        
//     }
int main() {

return 0;
}