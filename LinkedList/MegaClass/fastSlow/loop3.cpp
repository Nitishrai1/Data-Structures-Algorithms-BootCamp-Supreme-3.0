// remove loop in ll
#include<bits/stdc++.h>
using namespace std;
//   void removeLoop(Node* head) {
//         // code here
//         // just remove the loop without losing any nodes
//         Node* slow=head;
//         Node* fast=head;
//         if(head==nullptr)return;
//         while(fast && fast->next){
//             slow=slow->next;
//             fast=fast->next->next;
//             if(fast==slow){
//                 break;
//             }
//         }
//         if(!fast || !fast->next){
//             return;
//         }
//         // ab slow or fast same pos pe point kar rahe hai

//         slow=head;
     
//         while(slow!=fast){
//             slow=slow->next;
          
//             fast=fast->next;
//         }
//     //   ab yaha par fasst slow starinbg ko point kar raha hai
//         while(fast->next!=slow){
//             fast=fast->next;
            
//         }
//         fast->next=nullptr;
//     }
int main() {

return 0;
}