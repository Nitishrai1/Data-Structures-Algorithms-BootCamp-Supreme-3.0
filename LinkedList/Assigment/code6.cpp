// flattern a ll'
#include<bits/stdc++.h>
using namespace std;
// Node* merge(Node* a,Node* b){
//         if(!a)return b;
//         if(!b)return a;
        
//         Node* ans=0;
//         if(a->data<b->data){
//             ans=a;
//             ans->bottom=merge(a->bottom,b);
//         }
//         else{
//             ans=b;
//             ans->bottom=merge(a,b->bottom);
//         }
//         return ans;
//     }
//     Node *flatten(Node *root)
//     {
//         // Your code here
//         // sabse pahle do sublist nikalo or merge kar do
//         // fir res se dusara sublist merge kar do
//         if(root==NULL)return 0;
//         Node* mergedll=merge(root,flatten(root->next));
//         return mergedll;
        
//     }
int main() {

return 0;
}